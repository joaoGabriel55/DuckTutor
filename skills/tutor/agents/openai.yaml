interface:
  display_name: "DuckTutor"
  short_description: "Understand and own every AI-assisted change"
  brand_color: "#F4B942"
  default_prompt: "Use $tutor with this project's instructions and skills, then guide the change through its deterministic learning gates."

policy:
  allow_implicit_invocation: true
